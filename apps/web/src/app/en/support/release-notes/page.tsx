/*
 * English route. Generated by scripts/generate-en-routes.py — edit the
 * Bengali page at app/(public)/support/release-notes instead, then re-run it.
 */
/* eslint-disable @typescript-eslint/no-explicit-any */

import BengaliPage from '../../../(public)/support/release-notes/page';
import { metadata as bengaliMeta } from '../../../(public)/support/release-notes/page';
import { englishMetadata } from '@/lib/i18n/metadata';

export const metadata = englishMetadata(bengaliMeta, '/support/release-notes');
export default function EnglishPage(props: any) {
  // The same component, told which language to render in.
  return <BengaliPage {...props} locale="en" />;
}
